ru:
  date:
    order:
      - year
      - month
      - day
    formats:
      default: '%m/%d/%Y'
      with_weekday: '%a %m/%d/%y'

  time:
    formats:
      default: '%a, %b %-d, %Y at %r'
      date: '%b %-d, %Y'
      short: '%B %d'
      long: '%a, %b %-d, %Y at %r'
  back: Назад
  access:
    denied: Доступ запрещен
  errors:
    messages:
      blank: Поле не может быть пустым
